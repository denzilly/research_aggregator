services:
  phage-digest:
    build: .
    container_name: phage-digest
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    networks:
      - web

networks:
  web:
    external: true
